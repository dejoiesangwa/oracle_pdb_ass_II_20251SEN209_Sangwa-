# oracle_pdb_ass_II_20251SEN209_Sangwa-
# Oracle Pluggable Database Management – ​​Assignment II ## 1. Overview This assignment focused on Oracle Pluggable Database (PDB) management. The main activities completed were: - Creating a new Pluggable Database (PDB) - Creating a user inside the PDB - Opening and verifying the PDB - Creating and deleting a temporary PDB - Accessing Oracle Enterprise Manager Database Express - Documenting the completed tasks with screenshots --- ## 2. Oracle Environment Used - Oracle Database: Oracle Database 21c Enterprise Edition - Oracle Version: 21.0.0.0.0 - Command Line Tool: SQL*Plus - Database Management Tool: Oracle Enterprise Manager Database Express - Operating System: Windows - Main Container Database: ORCL --- ## 3. Task 1 – Create a New Pluggable Database For this task, I created a new Pluggable Database named: **SA_PDB_20251SEN209** I also created the following user inside the PDB: **SANGWA_PLSQLAUCA_20251SEN209** After creating the PDB, I opened it and verified that its open mode was **READ WRITE**. I also verified that the required user existed inside the PDB. Screenshots showing the PDB creation, open state, and created user are available in the `screenshots/pdb_creation` folder. --- ## 4. Task 2 – Create and Delete a Temporary PDB For this task, I created a temporary Pluggable Database using the required naming format. After creating it, I verified that the PDB existed. I then closed the temporary PDB and deleted it using the `INCLUDING DATAFILES` option. Finally, I verified that the temporary PDB no longer existed in the database. Screenshots showing the creation and deletion are available in the `screenshots/pdb_deletion` folder. --- ## 5. Task 3 – Oracle Enterprise Manager For this task, I configured and accessed Oracle Enterprise Manager Database Express. The OEM dashboard was used to view the Oracle database environment and the Pluggable Database created during the assignment. The dashboard also displayed the database user information. A screenshot of the OEM dashboard is available in the `screenshots/oem_dashboard` folder. --- ## 6. Challenges Faced ### SHOW PDBS command The `SHOW PDBS` command was not available in my SQL*Plus environment. I solved this by using the `V$PDBS` view to check the PDB names and their open states. ### Insufficient privileges When I first tried to open a PDB while connected as the `SYSTEM` user, Oracle returned an insufficient privileges error. I solved this by connecting using the `SYSDBA` administrative privilege before performing PDB management operations.### Oracle Enterprise Manager access I experienced difficulties accessing the PDB through Oracle Enterprise Manager. I checked the HTTPS configuration and configured an EM Express HTTPS port for the PDB before accessing it through the browser. --- ## 7. Integrity Statement I confirm that I personally performed the practical Oracle database activities documented in this repository and captured the screenshots from my own Oracle environment. The screenshots and database results included in this repository reflect the work that I executed during this assignment. --- ## 8. Submission Details **Repository Link:** [Insert your GitHub repository URL] **PDB Name Created:** SA_PDB_20251SEN209 **Issues Encountered:** Yes ``` A few im:contentReference[oaicite:2]{index=2}
